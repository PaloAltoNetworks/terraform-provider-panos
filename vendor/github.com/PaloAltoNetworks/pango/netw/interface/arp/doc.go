/*
Package arp is the client.Network.Arp namespace.

If you're configuring arp for a VLAN interface, leave the iName param empty and
specify the vlan name (eg: vlan.1) as the subName.

Normalized object:  Entry
*/
package arp
