package data

// Valid values for Rule.Direction.
const (
	DirectionUpload   = "upload"
	DirectionDownload = "download"
	DirectionBoth     = "both"
)

const (
	singular = "data filtering security profile"
	plural   = "data filtering security profiles"
)
